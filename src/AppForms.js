// https://reactjs.org/docs/forms.html
// with Controlled Forms, what is displayed in our Form matches our State && State controls what is inside of form elements
// , so value={this.state.<...>}

import React from "react"
import Form from './components/FormContainer'

function App() {
    return (
        <Form />
    )
}

export default App